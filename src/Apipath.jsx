const ApiPath = {
    FirebaseApi : 'https://react-d206f-default-rtdb.firebaseio.com/api.json'
}
export default ApiPath;
// can use this in any component if multiple api are used in the project we have to use and write all api inside object and export it and in import while using it we have to use dot notation to access the api like this: import ApiPath from '../Apipath' and then use it like this ApiPath.products